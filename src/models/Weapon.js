/**
 * Builds a weapon using a name (string), a damage (number), a visual (filepath)
 */
class Weapon {
    constructor(name, damage, picture) {
        this.name = name
        this.damage = damage
        this.picture = picture
    }
}